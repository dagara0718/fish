# v1.3 analyze — map location selection → decision detail panel

Manual analyze pass (no `speckit-analyze` tool available in this session). Checks requirement↔task,
task↔code, spec↔UI, and the three-model semantic separation this delta depends on.

## CRITICAL / HIGH

None found.

## Requirement ↔ Task ↔ Code

| Requirement | Task | Code | Verified by |
|---|---|---|---|
| REQ-FUNC-MAP-004 (arbitrary click, no marker/background conflict) | T067 | `naver-map-provider.ts` (time-guard + stopPropagation) | `tests/unit/naver-map-provider.test.ts`, `e2e/live-map.spec.ts` "marker click does not also register..." |
| REQ-FUNC-MAP-005 (single replace-not-accumulate arbitrary marker) | T067 | `naver-map-provider.ts` `update()` clears markers each call | `tests/unit/naver-map-provider.test.ts` "replaces markers on each update" |
| REQ-FUNC-MAP-006 (nearest candidates, capped 5, ascending, no auto-select) | T068 | `LiveApp.tsx` `onMapBackgroundClick` + `rankLocationCandidates` | `e2e/live-map.spec.ts` "arbitrary map click surfaces nearest..." |
| REQ-FUNC-MAP-007 (list↔map sync, fixed marker vocabulary) | T068 | `LiveApp.tsx` `previewId` prop, `naver-map-provider.ts` `map-marker--preview` class | `tests/unit/naver-map-provider.test.ts` "marks the previewed official marker..." |
| REQ-NFR-MAP-PERF-001 (no index API call before confirm) | T068 | `LiveApp.tsx` `onMapBackgroundClick` calls `getCatalog` only, `select()` calls `getOfficialIndex` only on confirm | `e2e/live-map.spec.ts` `perPointCalls()` assertions (0 before confirm, >0 after) |
| REQ-FUNC-ACCESS-002 (CHECK_REQUIRED default, never ALLOWED without evidence) | T069 | `fishing-access.ts` `UnverifiedFishingAccessProvider` | `tests/unit/fishing-access.test.ts`, `e2e/live-map.spec.ts` "never shows fishing access as allowed" |
| REQ-FUNC-ACCESS-003 (typed provider boundary) | T069 | `fishing-access.ts` `FishingAccessProvider` interface | Type-checked via `tsc -b` |
| REQ-FUNC-SPECIES-001 (plain species summary, no probability language) | T070 | `OfficialIndexPanel.tsx` `.species-summary` block | `tests/component/official-index.test.tsx` "renders a plain species summary..." |
| REQ-NFR-DETAIL-001 (no internal identifiers in user copy) | T070 | All new/edited JSX in `LiveApp.tsx`, `AccessStatusPanel.tsx`, `OfficialIndexPanel.tsx` | Manual read (see below) |

All T067–T070 tasks map to existing files only; no v1.2 module (`live-provider.ts`, `naver-map-loader.ts`
event *typing* excepted, Worker, Demo `App.tsx`) was rewritten. `naver-map-loader.ts`'s only change is
widening `Event.addListener`'s handler type from `() => void` to `(event?: unknown) => void` — required
for the new click-payload contract, not a rewrite of the SDK-loading logic itself.

## Spec ↔ UI

Read `LiveApp.tsx`, `AccessStatusPanel.tsx`, `OfficialIndexPanel.tsx` JSX directly for banned tokens:
no `REQ-`, `T0`, `Fixture`, `Provider`, `DTO`, `UNSUPPORTED_POINT_INTERNAL`, or similar identifiers
appear in any user-facing string. Confirmed by grep:

```
grep -noE "REQ-[A-Z-]+|T0[0-9]{2}|Fixture|DTO|Provider" src/app/LiveApp.tsx src/features/official-index/*.tsx
```
returns no matches inside JSX text nodes (only inside `// comments` and TypeScript identifiers, which
are not rendered).

## Trust / Access / Demo-Live semantics

- `TrustStatus` (`domain/contracts.ts`) — data collection/freshness. Used only on `OfficialSpeciesIndex`
  and `EnvironmentalObservation`, unchanged from v1.2.
- `FishingAccessStatus` (`official-index/fishing-access.ts`) — new, separate type, separate component
  (`AccessStatusPanel`), separate CSS namespace (`.access-status-*`). Never read from or written into
  `TrustStatus` or `OfficialSpeciesIndex.officialGrade`.
- Official grade/score (`officialGrade`/`officialScore` on `OfficialSpeciesIndex`) — unchanged, sourced
  only from parsed `totalIndex`/`lastScr`.
- No code path sets `FishingAccessStatusKind` to anything other than `CHECK_REQUIRED` in this release;
  grep confirms `UnverifiedFishingAccessProvider` is the only class implementing
  `FishingAccessProvider`, and it always returns `{ status: 'CHECK_REQUIRED', evidence: [] }`.
- Demo/Live: `App.tsx` (fixture) and `LiveApp.tsx` (map/proxy) remain fully separate components;
  `LiveApp.tsx`'s `onMapBackgroundClick`/`select()` never call `getDemoOfficialIndex` and there is no
  fallback path from a Live failure into fixture data (`e2e/live-map.spec.ts` "Live proxy failure never
  displays demo fallback", pre-existing from v1.2, still passes unmodified).

## Result

Implementation may proceed / has proceeded past this checkpoint. CRITICAL: 0, HIGH: 0.
