# v1.6.3 analyze — tidal-current forecast in the live brief

Manual cross-artifact analyze (no `speckit-analyze` tool in this session, consistent with v1.3–v1.6),
plus an independent read-only diff review subagent (result: no issues).

## CRITICAL / HIGH

None remaining. One HIGH found and fixed in the uncommitted draft before this pass closed:

- **HIGH (fixed) — unsourced time basis.** The draft built the request window with `getUTC*` and
  justified it as "more conservative", contradicting its own `timeBasis: 'UNCONFIRMED'` contract. If
  KHOA is KST, the app would have shown a window 9 h in the past as the current forecast. Replaced by
  `requestWindow()` (covers the present under both readings) and a UI that never names a "now" row.
- **MEDIUM (fixed)** — the panel returned `null` for `NOT_CONNECTED` and swallowed exceptions, so a
  misconfigured deploy looked identical to "no feature". Now finite text states.
- **MEDIUM (fixed)** — marine shared the index `AbortController`, so a cancelled marine request could
  leave `marineBusy` true and a marine retry would have cancelled the index. Now separate controllers.
- **LOW (fixed)** — `전류` was mapped to `INSTANTANEOUS`; live data shows it is the current's turn.

## Requirement ↔ Task ↔ Code ↔ Verification

| Requirement | Task | Code | Verified by |
|---|---|---|---|
| REQ-FUNC-MARINE-UI-001 explicit trigger, official coord, no GPS | T101 | `LiveApp.tsx` `loadMarine` called only from `select`/panel retry | e2e "fetches only after explicit selection…" (0 calls after search/GPS/preview; `lat=35.000`, no `35.01`; storage empty) |
| REQ-FUNC-MARINE-UI-002 finite states | T100/T101 | `MarineCurrentPanel.tsx`; provider `NOT_CONNECTED` for bad base URL | component tests (loading/not configured/unsupported/failure/partial/stale); e2e 503 → not configured |
| REQ-FUNC-MARINE-UI-003 provenance | T100 | TrustBadge section + per row; `<details>` evidence | component "keeps an empty type…"; e2e keyboard Enter opens table |
| REQ-FUNC-MARINE-UI-004 independence/cancellation | T101 | `marineRequest` ref; `start()` and unmount abort | e2e failure isolation + marine-only retry (index calls unchanged), late-response race, re-search cancel |
| REQ-FUNC-MARINE-UI-005 time basis TBD | T099/T100 | `requestWindow`, `timeBasis`, panel note | unit "contains the real present under both…", rollover test; component "never labels a row as the present one" |
| REQ-FUNC-MARINE-UI-006 empty type | T099 | `normalizeTidalCurrent` | unit type="" kept, unknown label → PARTIAL; component "미표기" |
| REQ-FUNC-MARINE-UI-007 staleness | T100 | `MARINE_STALE_AFTER_MS`, 30 s tick | component STALE + retry; fake-timer "becomes STALE while left open" |
| REQ-NFR-MARINE-004 config boundary | T101 | `deploy-pages.yml` `VITE_MARINE_API_BASE_URL: vars.MARINE_API_BASE_URL` | `npm run secret:scan` OK; bundle grep (see result) |

## Constitution checks

- TrustStatus: 5 values unchanged; STALE is shown *alongside* UNVERIFIED, never replacing it (no lossy mapping).
- Partial-failure-first: marine failure never hides the official index (e2e).
- No demo fallback on live marine failure. No species/recommendation/safety wording (component + existing species-guidance e2e F).
- Existing Requirement meanings unchanged; no existing test deleted or weakened (one expected enum value renamed `INSTANTANEOUS` → `SLACK`, same assertion).
- Open decisions stay open: time basis, direction convention, unsupported-area detection all remain TBD.

## Result

CRITICAL: 0. HIGH: 0 (after fixes). Implementation may be considered complete for T098–T103.
