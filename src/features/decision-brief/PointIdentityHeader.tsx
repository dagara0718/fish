import type { FishingPoint } from '../../domain/contracts'
export function PointIdentityHeader({ point, onBack, generatedAt }: { point: FishingPoint; onBack: () => void; generatedAt?: string }) { return <header className="brief-header"><div><p className="identity-kicker">선택한 포인트 · DEMO</p><h2 className="identity-title">{point.name}</h2><p className="identity-context">{point.regionContext} · {point.pointType?.replace('합성 ', '') ?? '유형 미정'}</p><div className="brief-meta"><span><i aria-hidden="true"></i> 데이터 확인 상태 표시</span><span className="mono">기준 {generatedAt ?? '확인 필요'}</span></div></div><button className="secondary-button" type="button" onClick={onBack}>다른 포인트 찾기</button></header> }
